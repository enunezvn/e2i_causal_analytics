# SUPERSEDED — use ../.env.example. This file is not read by anything.
# Compose resolves ${VAR} through docker/.env, which is a symlink to ../.env
# (create it with: cd docker && ln -sf ../.env .env).
