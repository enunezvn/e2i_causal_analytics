"""YAML scenario-config loader (shard 06).

Loads ``tests/configs/scenarios/{a,b,c}.yaml`` (schema_version
``synthetic_v2.scenario.v1``) and resolves to a ``ScenarioBuilder``
subclass. Roundtrip-tested in ``tests/ml/synthetic_v2/test_yaml_configs.py``.

Filled in by commit 10.
"""
