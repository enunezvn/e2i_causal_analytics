"""Unit tests for orchestrator agent.

Test coverage:
1. Intent classification (pattern matching + LLM fallback)
2. Routing logic (single and multi-agent dispatch)
3. Parallel agent dispatch with timeouts
4. Multi-agent response synthesis
5. Integration tests (complete workflow)
"""
