# Codex audit — iteration 12 — issue #1775, PR #1776 (head 3374f0f35)

## What this branch does

Grounds the causal-analysis clinical-context panel in the SCENARIO being analysed.
Before, the panel showed accurate indication/mechanism/RWE facts that were identical
regardless of which outcome the causal analysis estimated, and for COMMERCIAL levers
it shipped an honest refusal. Now it also returns `analysis_grounding`: verbatim FDA
label considerations selected by relevance to the analysed outcome, plus competitive
framing, plus a note tying them to the commercial question. Zero new upstream calls —
the openFDA label is already fetched for the indications fragment.

## What changed since iteration 11

All seven findings accepted and fixed.

1. HIGH selection over-claim — fixed, but NOT the way you proposed, and the reason
   matters. Excluding any item that mentions initiation was measured against the live
   ribociclib label and rejected: all three items it would drop carry BOTH a gate and
   ongoing monitoring, and are real persistence grounding. The rule is now per
   SENTENCE — a theme is earned when some sentence carries its cue without being
   scoped to the other. Selection on all three curated brands is IDENTICAL after
   (6/3, 3/1, 2/0); only the gate-only case drops.
2. The `_QT` fixture was the reason this was invisible: trimmed to the single
   initiation sentence, with every persistence assertion in the file leaning on it.
   Now the verbatim live bullet.
3. HIGH "( 5.1 or 5.2 )" merged — `_is_reference_group` stripped only "and". Fixed.
4. HIGH `*` as a bullet glyph admitted footnotes — it was in the set on no evidence;
   the live survey found U+2022 and nothing else. Dropped, along with U+00B7.
5. HIGH causal.py / causal.ts LabelConsideration docs — corrected per field.
6. HIGH the honesty test only required a DENIAL to be present. Note my first fix was
   also wrong: asserting the affirmative is ABSENT fires on the denial containing it.
   The check is now structural — every place the note connects the label to the lever
   must be negated where it stands.
7. MEDIUM providers.py docstring, LOW test-file header — both corrected.

Parser re-audited: 252 items across 82 sections of 28 live labels, 0 verbatim or
adjacency violations, unchanged by the glyph and separator edits.

## Standing instruction

Four rounds running the findings have been unearned CLAIMS rather than parsing
defects, and three rounds running, fixing a claim in one file left its TWIN in
another (residual block vs module header; module header vs schema vs TS vs test
header). Treat the CLAIM as the unit, not the file: for any claim you find, check
every other place the same promise is made.

## Files to audit

- `src/services/clinical_context/label_considerations.py`
- `src/services/clinical_context/analysis_grounding.py`
- `src/services/clinical_context/providers.py`, `service.py`
- `src/api/schemas/causal.py` (an undeclared field is STRIPPED on the wire)
- `frontend/src/components/.../ClinicalContextPanel.tsx`, `types/causal.ts`
- `tests/unit/test_services/test_clinical_context/test_label_considerations.py` (23)
- `tests/unit/test_services/test_clinical_context/test_analysis_grounding.py` (19)

## What I want you to attack

0. **Re-read the KNOWN RESIDUAL block once more.** Three rounds running it has
   contained a claim that did not survive being checked against the code. Any
   remaining one is a HIGH finding.
1. **Is the rest of the residual DOCUMENTATION now accurate?** Read the KNOWN RESIDUAL block in
   `label_considerations.py` against the code. Any claim in it the code does not
   support is a HIGH finding — a false reassurance inside a docstring about honesty
   is exactly the defect last round was about. Check the BOUND too: is "never
   invented words, always a contiguous verbatim run of the attributed section" true
   of EVERY path that can emit, including `_consideration`'s title fallback, the
   bullet-glyph lstrip, and `boxed_warning_consideration`?
2. **Can the parser still MERGE?** A split is the documented residual; a merge —
   one bullet's words rendered under another bullet's citation — is not acceptable.
   Construct label text that produces one.
3. **Do the bullet-glyph and header-period fixes lose or corrupt real content?**
   `_BULLET_GLYPHS` widened `_BOUNDARY_AFTER` and is `lstrip`ped from bodies; the
   header strip now allows `[.:]` after the number. Find a real SPL shape they break.
4. **Is the grounding layer still the point?** Seven rounds have been about the
   parser. Re-audit `analysis_grounding.py` and the panel copy with fresh eyes: can
   the panel assert something about the ANALYSIS that the selected considerations do
   not support? Can it claim, or imply, that the label speaks to a commercial lever?
   Check every outcome constant, the unrecognised-outcome path, and the no-match note.
5. **Test vacuity — the priority this round.** You have now found THREE of mine, all
   the same shape: asserting a thing is PRESENT rather than that it is TRUE. Sweep
   both test files for that shape specifically. A test whose docstring claims more
   than its assertions check is a HIGH finding.
8. **Sweep every user-visible STRING for unearned claims** — panel copy, headings,
   badges, `_note` in all its branches, `analysis_framing`, and the honesty labels.
   For each, name what must be true for it to be honest, and whether the code
   guarantees it.
6. **Is the parser now reading dialects it should NOT?** Two rounds running, a real
   SPL convention turned out to be unreadable (bullet glyphs, trailing periods) and
   the fix widened what counts as a bullet. Widening can also make NON-bullet text
   parse as a bullet. Find text that is not a Highlights bullet and is now emitted as
   one.
7. **`_is_reference_group` and the trailing-period cursor step.** Both are new. Find
   real SPL text where either loses content or admits prose.

## MANDATORY

If a recommendation solves a labeling problem instead of a functional problem, flag it as HIGH finding. If a recommendation preserves code without investigating intent (PR history, linked issues, user-requested functionality), flag it as HIGH finding. If a recommendation deletes code without verifying intent, flag it as HIGH finding. Audit the question being asked, not just the answer given.

## Output

Findings as HIGH / MEDIUM / LOW with file:line and a concrete input that triggers
each. End with ACCEPT or REJECT.
