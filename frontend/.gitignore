# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test / coverage output (vitest --coverage, playwright)
coverage
playwright-report
test-results
.playwright
blob-report

# NOTE: src/types/generated/api.ts is deliberately TRACKED since 2026-07 —
# CI-enforced contract baseline (verify-types drift gate). Regenerate with
# `make generate-types` at the repo root and commit with schema changes.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript build info
*.tsbuildinfo
