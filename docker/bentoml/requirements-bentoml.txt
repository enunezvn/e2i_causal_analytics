# BentoML Model Serving Dependencies
# Core framework
# Exact pin to match root requirements.txt and prevent silent version skew on
# Docker rebuild (Issue #321, surfaced by PR #315 / wave 2026-05-17).
bentoml==1.4.39

# ML frameworks
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
numpy>=1.24.0
pandas>=2.0.0

# Causal inference
econml>=0.14.0
# dowhy < 0.13 calls nx.algorithms.d_separated, removed in networkx 3.5
# (#869); 0.13+ imports is_d_separator with a fallback. Keep in lockstep with
# pyproject.toml (guarded by tests/test_requirements_lock.py).
dowhy>=0.13

# API and validation
pydantic>=2.0.0
fastapi>=0.100.0
uvicorn>=0.23.0

# HTTP client for Feast online-features lookup (Block 3A)
# Used by scripts/bentoml/e2i_serving_service.py to fetch features from
# the Feast HTTP endpoint (FEAST_HTTP_ENDPOINT or FEAST_URL) when a request
# supplies entity_ids + feature_view instead of a raw features matrix.
httpx>=0.27.0

# Monitoring
prometheus-client>=0.17.0

# Utilities
PyYAML>=6.0
python-json-logger>=2.0.0
