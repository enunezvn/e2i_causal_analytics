"""Shard 08 synthetic build artifacts.

- ``allocation_builder`` — per-HCP-CATE -> resource_optimizer allocation_targets.
- ``ensemble_trainer`` — >=2 diverse models per (cohort,brand) + deployment_manifest.
"""
