"""Public API surface (shard 01 §B).

Will host:
- ``generate_scenario(scenario, *, seed, n_total, train/val/test ratios)`` (B.3)
- ``SyntheticDataset`` frozen dataclass (B.2)
- ``ScenarioMetadata`` frozen dataclass (B.2)

Filled in by commit 06.
"""
