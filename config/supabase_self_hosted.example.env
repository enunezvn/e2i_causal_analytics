# =============================================================================
# E2I Application - Self-Hosted Supabase Configuration
# =============================================================================
# Use these environment variables when running with self-hosted Supabase.
# Copy relevant values to your .env file.
#
# This replaces the Supabase Cloud configuration.
# =============================================================================

# Supabase API Configuration (for application use)
SUPABASE_URL=http://localhost:8000
SUPABASE_ANON_KEY=your-anon-key-from-self-hosted
SUPABASE_SERVICE_KEY=your-service-role-key-from-self-hosted

# Direct PostgreSQL Connection (for migrations and admin tasks)
# Use port 5433 as configured in docker-compose.override.yml
DATABASE_URL=postgresql://postgres:your-password@localhost:5433/postgres

# Frontend Configuration (for React app)
VITE_SUPABASE_URL=http://138.197.4.36:8000
VITE_SUPABASE_ANON_KEY=your-anon-key-from-self-hosted

# =============================================================================
# Additional Notes
# =============================================================================
#
# Port Mappings:
#   - 8000: Kong API Gateway (Supabase API)
#   - 5433: PostgreSQL (mapped from internal 5432)
#   - 3001: Supabase Studio (mapped from internal 3000)
#
# For droplet deployment, replace localhost with 138.197.4.36
#
# Generate keys at: https://supabase.com/docs/guides/self-hosting/docker#generate-api-keys
# =============================================================================
