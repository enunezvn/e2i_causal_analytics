# E2I BentoML Model Serving Configuration
# =========================================

# BentoML home directory (where models are stored)
BENTOML_HOME=/home/enunez/bentoml

# Port for BentoML service (matches client defaults)
BENTOML_PORT=3000

# Model selection (optional — if unset, auto-discovers latest model)
# Pin to a specific model tag:
# E2I_BENTOML_MODEL_TAG=tier0_abc123:v5
#
# Use latest version of a named model:
# E2I_BENTOML_MODEL_NAME=tier0_tier0_e2
