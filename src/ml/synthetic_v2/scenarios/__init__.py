"""Scenarios sub-package (shard 01 §B.1).

Will host:
- ``ScenarioName`` enum with canonical IDs and ``from_short("A"/"B"/"C")`` resolver
- scenario registry dict mapping enum → builder class

Filled in by commit 05.
"""
